# 开发记录

## 0.1.0（2026-09-12）

完成第一个 Web 控制版本：使用 uHTTPd 提供局域网页面，通过两个固定的 CGI POST 接口调用现有 `picanlog start` 和 `picanlog stop`，实现 CAN 日志的启动与停止；服务默认监听 `8080` 端口，并由 systemd 设置为开机启动。Raspbian 10 软件源没有 uHTTPd 包，因此从 OpenWrt 官方源码构建：uHTTPd `3abcc89103799aaa79870fffcd58ec4370815024`、libubox `66195aee50424cbda0c2d858014e4cc58a2dc029`，关闭 TLS、Lua 和 UBus 功能。本版本暂不提供状态、日志查看、下载和登录功能。

## 0.1.1（2026-09-12）

新增“查看状态”按钮并调用 `picanlog status`；启动、停止和查看状态三个操作改为页面内异步请求，执行结果统一显示在按钮区域下方，不再跳转到单独的结果页面。

## 0.1.2（2026-09-12）

在“查看状态”下方新增“查看日志”按钮；点击后在同一信息显示区列出日志目录中的全部 `can-*.log` 文件，点击文件名可通过受限 CGI 接口下载。当前不提供分页、在线预览或删除功能。

## 0.1.3（2026-09-12）

为树莓派增加车载 Wi-Fi 自动连接配置：保留现有家中 Wi-Fi 为高优先级网络，并加入 iPhone 热点 `“hwy”的 iPhone` 作为低优先级自动连接网络；热点密码仅保存于树莓派系统配置，不进入项目文档或代码。

## 0.1.4（2026-09-12）

在 Web 页面增加带二次确认的“关闭树莓派”按钮，通过固定 CGI 接口提交延迟关机命令；同时在 `picanlog status` 中增加当前 Wi-Fi SSID 和 `wlan0` IPv4 地址，方便手机接入同一热点后确定 Web 访问地址。
